<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a71568.persondoctor">

    <!-- 权限 好啊 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".util.AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/doctor_ico"
        android:label="私人医生"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.BeforeActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.RegisterActivity" />
        <activity
            android:name=".activity.BaseActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".activity.MenuActivity" />
        <activity android:name=".activity.SelectDoctor" />
        <activity android:name=".activity.DoctorDepartment" />
        <activity android:name=".activity.Community" />
        <activity android:name=".testForNewJob.RyActivity"></activity>
    </application>

</manifest>